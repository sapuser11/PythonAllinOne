---
applications:
- name: ats-teams-bot
  path: .
  routes:
    - route: ats-teams-bot.cfapps.us10-001.hana.ondemand.com
  buildpacks:
  - python_buildpack
  memory: 1G
  command: uvicorn main:app --reload --host 0.0.0.0 --port ${PORT}